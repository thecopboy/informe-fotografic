# Plantilla de configuració del Webhook de GitHub
# Copia aquest fitxer a .env i actualitza els valors

# Servidor Webhook
PORT=3000
NODE_ENV=production

# GitHub Webhook Secret
# GENERAR amb: openssl rand -base64 32
# Ha de coincidir amb el secret configurat a GitHub Settings
# Veure ../docs/WEBHOOK_GITHUB_SETUP.md per instruccions completes
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret-min-32-chars
