# Plantilla de configuració d'entorn
# Copia aquest fitxer segons l'entorn:
#   - .env.dev (desenvolupament local/codespaces)
#   - .env (producció o general)

# Servidor
PORT=33333
NODE_ENV=production
HOST=localhost

# Base de dades
DB_PATH=./database/app.db

# JWT Secrets
# GENERAR amb: openssl rand -base64 48
# IMPORTANT: MAI utilitzar els mateixos secrets en diferents entorns!
JWT_ACCESS_SECRET=your-super-secure-access-secret-min-48-chars
JWT_REFRESH_SECRET=your-super-secure-refresh-secret-min-48-chars

# Webhook GitHub (auto-deploy)
# NO definir-la aquí. Usa el fitxer dedicat: git-update/.env
# Guia: docs/WEBHOOK_GITHUB_SETUP.md

# ============================================
# INSTRUCCIONS PER CONFIGURAR PER ENTORN
# ============================================
#
# DESENVOLUPAMENT (Codespaces/Local):
#   cp .env.example .env.dev
#   # Genera secrets i actualitza .env.dev
#   openssl rand -base64 48  # Per JWT_ACCESS_SECRET
#   openssl rand -base64 48  # Per JWT_REFRESH_SECRET
#   npm run dev  # Utilitza .env.dev automàticament
#
# PRODUCCIÓ:
#   # Genera secrets directament al servidor
#   export JWT_ACCESS_SECRET=$(openssl rand -base64 48)
#   export JWT_REFRESH_SECRET=$(openssl rand -base64 48)
#   # O crea un .env al servidor amb els secrets
#   npm start  # Utilitza .env
#
# IMPORTANT:
# - Cada entorn (dev, staging, producció) ha de tenir els seus propis secrets
# - MAI compartir els secrets entre entorns
# - MAI pujar els secrets a GitHub
# - Canviar els secrets regularment (cada 3-6 mesos)

